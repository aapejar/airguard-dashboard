/**
 * This module previously held mock data for offline development.
 * The production build talks to the AirGuard backend exclusively — no mock data,
 * no demo users, no fake telemetry. File kept (empty) to preserve import paths.
 */
export {};
